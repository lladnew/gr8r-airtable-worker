{
  "name": "gr8r-airtable-worker",
  "main": "index.js",
  "compatibility_date": "2025-06-12",

  "routes": [
    {
      "pattern": "api.gr8r.com/api/airtable*",
      "zone_name": "gr8r.com"
    }
  ]

  // ❌ Do NOT include `services`, `vars`, or `secret_stores`
  // ✅ Manage bindings manually in the dashboard (GRAFANA_WORKER, SECRETS)
}
